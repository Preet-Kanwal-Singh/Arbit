# Open Questions

Current research questions that remain unanswered, or were generated by
completed work. Maintained by Desktop Claude A (Ledger Keeper).

## Carried forward from `PROJECT_BASE_CONTEXT.md` §8 (still open, original order)

1. Recompute Episode 1's real beta range from the frozen snapshot, with full
   provenance. (The old "0.20 to 1.91 across 22 months" figure is discarded —
   untraceable.)
2. Real phi_β for the TCS/INFY beta process. Prior candidate — 15-day-sampled
   phi ≈ 0.77, CI [0.66, 0.87] — is untested in the actual FNR grid, not
   confirmed.
3. Where FNR vs. phi_β actually thresholds between 0.90 and 0.98 — untested
   range, needs a dense grid, not two points.
4. Adversarially test the phi regime-dependence finding (pre-COVID ~0.95,
   post-COVID ~0.99) — nobody has tried to break it yet.
5. Was "excise the instability window" ever tested against global re-fitting,
   or did the investigation narrow to Option B without that comparison being
   run?

## From the cointegration window-length validation work (2026-07-04)

6. ~~Tier classification of this work~~ — **RESOLVED.** Preet confirmed Tier B:
   an exploratory hit-and-trial pass over window lengths, not a
   decision-gating claim, with Codex + Antigravity both run in parallel
   specifically to have a cross-check, not to produce a formal Tier A
   admission. See `decisions.md`.
7. ~~Was a Spec Block ever written for this question~~ — **MOOT, given #6.**
   Tier B doesn't require one.
8. Codex's `provenance.json` cites git commit `606cc817c1de5fda7b57dc5814239e36592cc70e`,
   whose tree does not contain the frozen snapshot file (added three minutes
   later in commit `a82ed569...`, "Added Freezed csv"). Doesn't block
   anything for Tier B, but worth fixing if these numbers get reused: should
   Codex re-stamp provenance against a commit that actually contains the data
   it used?
9. Antigravity/Opus's run used a live yfinance pull instead of the frozen
   snapshot `tcs_infy_v1_2026-07-04`, and recorded no git commit. Not a
   blocker for Tier B, but worth deciding whether to redo it against the
   frozen snapshot if these numbers are relied on for anything beyond the
   initial hit-and-trial pass.
10. **Numeric disagreement between the two candidates — split after checking
    both scripts directly:**
    - *Part B real-data timeline (500d window, "6 months" vs "22 months" lead
      time ahead of the first FLAGGED label): RESOLVED.* Codex and Opus
      define "borderline" with different, explicit thresholds (Codex: EG or
      ADF p in `[0.05, 0.10)`, already-failing; Opus: EG p `> 0.03`,
      still-passing). Confirmed by reading the code. The one milestone both
      scripts define identically — "both tests fail" — lands within 2 days
      of each other (2023-12-29 vs 2023-12-31), supporting a definitional
      rather than substantive gap. See `worklog.md`.
    - *Part A synthetic FNR (500d window, near-unit-root, p < 0.05: Codex
      0.94 vs Opus 0.87 at the same nominal spread-noise level): STILL OPEN.*
      Part A has no milestones to mismatch — this is the same statistic
      under a synthetic DGP that differs between the two scripts (different
      trend-innovation std, different true beta, an extra x-noise term in
      Codex's version, different spread-sigma grids). Not resolved by the
      Part B explanation. Nobody has decided which DGP setup is preferred, or
      whether the 7-point gap matters for anything built on this number.
11. `VERIFIED_FACTS.md` does not exist anywhere in this repo. Not clear
    whether it was never created or was created and lost/untracked. Not
    created by me — nothing here is ready for admission and it wasn't asked
    for.
12. The frozen snapshot's adjustment policy (per its `metadata.json`) is
    `auto_adjust=True` in yfinance, no manual corporate-action handling. Per
    base context §6, this is a real source of disagreement between
    reproductions, separate from methodology. Antigravity used a different
    live pull rather than this snapshot, so some residual Part B noise could
    still be adjustment-policy / retroactive-restatement related — though
    #10's finding already explains most of the headline gap on its own.
13. If this window-length comparison work continues: should Codex's and
    Antigravity's synthetic Part A scripts be standardized on the same DGP
    parameters (trend-innovation std, true beta, presence/absence of the
    extra x-noise term, and the spread-sigma grid)? Right now a future
    disagreement between the two can't be distinguished from sampling
    variance, because the underlying simulations aren't the same experiment.
