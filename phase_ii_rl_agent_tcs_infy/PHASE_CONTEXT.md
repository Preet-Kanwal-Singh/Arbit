# Phase II — RL Agent (TCS/INFY) — Phase Context

Planned phase, not yet started. This file will carry this phase's own
investigation history and carry-forward status once work begins, in the same
spirit as Phase I's `PHASE_CONTEXT.md`. Note this phase reuses the TCS/INFY
frozen snapshot(s) under the shared `/data/snapshots/` — see
`PROJECT_BASE_CONTEXT.md` §6 — rather than creating a duplicate. Any
Phase I admitted fact this phase relies on (e.g. episode boundaries) should
be cited explicitly by claim_id in this phase's own Spec Blocks, not assumed
silently.
